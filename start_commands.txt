# First-time setup
cp .env.example .env

# Main app stack from repo root
docker compose up -d

# Infra only (postgres, redis, rabbitmq, minio)
npm run infra:up

# App + infra
npm run app:up

# App + infra + media
npm run media:up

# Everything including observability
npm run all:up
